@model Domain.Entities.Game

<div class="card">
    <div class="card-body bg-light">
        <h3>
            <strong>@Model.Name</strong>
            <span class="float-end badge bg-primary">@Model.Price.ToString("# руб")</span>
        </h3>
        <span class="lead">@Model.Description</span>
    </div>
</div>